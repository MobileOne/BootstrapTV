var Channel = Class({
    id          : null,
    displayName : null,
    icon        : null,

    initialize : function( id, displayName, icon){
        this.id          = id;
        this.displayName = displayName;
        this.icon        = icon;
    }
});